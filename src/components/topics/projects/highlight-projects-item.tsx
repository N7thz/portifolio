import { ProjectsItemProps } from "@/@types"
import { FC } from "react"

export const HighlightProjectsItem:FC<ProjectsItemProps> = ({ 
    project
}) => {

    return (

        <div>highlight-projects-item</div>
    )
}
